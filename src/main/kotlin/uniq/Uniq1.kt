package uniq

import java.io.*
import java.util.*


public class Uniq1(private val noCase: Boolean, private val uniqRows: Boolean, private val changes: Boolean,
                  private val noSym: Int, private val outputFileName: String, private val inputFileName: String) {


/*
    @Throws(IOException::class)
    fun main(writer: BufferedWriter) {
        val rf: BufferedReader
        rf = (if (inputFileName == null) {
            System.`in`
        } else {
            File(inputFileName)
        }) as BufferedReader
        var line: String? = null
        var num = 1
        while (rf.readLine())

    }
*/
}

